-- LocalScript

local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local player = Players.LocalPlayer

local firedCount = 0
local espTagged = {}
local espHighlights = {}
local espConnections = {}
local hideConnections = {}
local playerAddedConn = nil
local subLevelWatcher = nil

local LAP_FOLDERS = {"Lap1","Lap2","Lap3","Lap4"}

local weakTimer   = 0
local fakeTimer   = 0
local wraithTimer = 0
local wallsTimer  = 0

local clickEnabled     = false
local fakeEnabled      = false
local wraithEnabled    = false
local wallsEnabled     = false
local espEnabled       = false
local hideEnabled      = false
local promptEnabled    = false
local weakEnabled      = false
local altPromptEnabled = false

-- ============================================================
-- EXIT HELPERS
-- ============================================================

local function getExitPart()
    local sub = workspace:FindFirstChild("Sublevel")
    if not sub then return nil end
    local exitModel = sub:FindFirstChild("Exit")
    if not exitModel then return nil end
    local part = exitModel:FindFirstChild("exit") or exitModel:FindFirstChildWhichIsA("BasePart")
    return part and part:IsA("BasePart") and part or nil
end

local function getExitPrompt()
    local part = getExitPart()
    return part and part:FindFirstChildWhichIsA("ProximityPrompt") or nil
end

local function getAltExitPart()
    local sub = workspace:FindFirstChild("Sublevel")
    if not sub then return nil end
    for _, child in ipairs(sub:GetChildren()) do
        if child.Name:lower():find("alternate") then
            local part = child:FindFirstChild("exit") or child:FindFirstChildWhichIsA("BasePart")
            if part and part:IsA("BasePart") then return part end
        end
    end
    return nil
end

local function getAltExitPrompt()
    local part = getAltExitPart()
    return part and part:FindFirstChildWhichIsA("ProximityPrompt") or nil
end

-- ============================================================
-- HIDE PLAYERS
-- ============================================================

local function setCharacterVisible(char, visible)
    if not char then return end
    for _, part in ipairs(char:GetDescendants()) do
        if part:IsA("BasePart") then part.LocalTransparencyModifier = visible and 0 or 1 end
    end
end

local function applyHideToPlayer(p)
    if p == player then return end
    if p.Character then setCharacterVisible(p.Character, false) end
    hideConnections[p] = p.CharacterAdded:Connect(function(char)
        if hideEnabled then task.defer(function() setCharacterVisible(char, false) end) end
    end)
end

local function restorePlayer(p)
    if p == player then return end
    if p.Character then setCharacterVisible(p.Character, true) end
    if hideConnections[p] then
        pcall(function() hideConnections[p]:Disconnect() end)
        hideConnections[p] = nil
    end
end

-- ============================================================
-- ESP
-- ============================================================

local function highlightObject(obj)
    if espTagged[obj] then return end
    local h = Instance.new("Highlight")
    h.FillColor = Color3.fromRGB(255, 80, 80)
    h.OutlineColor = Color3.fromRGB(255, 80, 80)
    h.Adornee = obj
    h.Parent = workspace
    espTagged[obj] = h
    table.insert(espHighlights, h)
end

local function scanContainer(container)
    for _, obj in ipairs(container:GetDescendants()) do
        if obj:IsA("Model") or obj:IsA("BasePart") then highlightObject(obj) end
    end
end

local function clearESP()
    for _, c in ipairs(espConnections) do pcall(function() c:Disconnect() end) end
    espConnections = {}
    for _, h in ipairs(espHighlights) do pcall(function() h:Destroy() end) end
    espHighlights = {}
    espTagged = {}
end

-- ============================================================
-- SUBLEVEL WATCHER
-- ============================================================

local function startSubLevelWatcher()
    if subLevelWatcher then pcall(function() subLevelWatcher:Disconnect() end) end
    subLevelWatcher = workspace.ChildAdded:Connect(function(child)
        if child.Name == "Sublevel" then
            task.defer(function()
                if espEnabled then clearESP() scanContainer(child) end
                if wraithEnabled then
                    for _, obj in ipairs(child:GetDescendants()) do
                        if obj.Name:lower():find("wraith") then pcall(function() obj:Destroy() end) end
                    end
                end
                if weakEnabled then
                    for _, obj in ipairs(child:GetDescendants()) do
                        if obj.Name:lower():find("weak") then pcall(function() obj:Destroy() end) end
                    end
                end
            end)
        end
    end)
end

-- ============================================================
-- HEARTBEAT FUNCTIONS
-- ============================================================

local function fireClicks()
    for _, obj in ipairs(workspace:GetDescendants()) do
        if obj:IsA("ClickDetector") then
            local ok = pcall(fireclickdetector, obj)
            if ok then firedCount += 1 end
        end
    end
end

local function deleteFake()
    local sub = workspace:FindFirstChild("Sublevel")
    if not sub then return end
    for _, obj in ipairs(sub:GetDescendants()) do
        if obj.Name:lower():find("fake") then pcall(function() obj:Destroy() end) end
    end
end

local function deleteWraith()
    local sub = workspace:FindFirstChild("Sublevel")
    if not sub then return end
    for _, obj in ipairs(sub:GetDescendants()) do
        if obj.Name:lower():find("wraith") then pcall(function() obj:Destroy() end) end
    end
end

local function deleteFakeWalls()
    local sub = workspace:FindFirstChild("Sublevel")
    if not sub then return end
    local walls = sub:FindFirstChild("Walls")
    local fw = walls and walls:FindFirstChild("FakeWalls")
    if fw then
        for _, v in ipairs(fw:GetChildren()) do pcall(function() v:Destroy() end) end
    end
end

local function deleteWeak()
    local sub = workspace:FindFirstChild("Sublevel")
    if not sub then return end
    for _, obj in ipairs(sub:GetDescendants()) do
        if obj.Name:lower():find("weak") then pcall(function() obj:Destroy() end) end
    end
    for _, folderName in ipairs(LAP_FOLDERS) do
        local lap = sub:FindFirstChild(folderName)
        if lap then
            for _, obj in ipairs(lap:GetDescendants()) do
                if obj.Name:lower():find("weak") then pcall(function() obj:Destroy() end) end
            end
        end
    end
end

-- ============================================================
-- RAYFIELD WINDOW
-- ============================================================

local Window = Rayfield:CreateWindow({
    Name = "PM Hub  v1.4",
    LoadingTitle = "PM Hub",
    LoadingSubtitle = "8:20pm bad gaem trust 😔🥀",
    ConfigurationSaving = { Enabled = false },
    Discord = { Enabled = false },
    KeySystem = false
})

local CombatTab  = Window:CreateTab("Combat",  4483362458)
local VisionTab  = Window:CreateTab("Vision",  4483362458)
local UtilityTab = Window:CreateTab("Utility", 4483362458)

-- ============================================================
-- COMBAT TAB
-- ============================================================

CombatTab:CreateSection("Combat")

CombatTab:CreateToggle({
    Name = "Click Detectors",
    CurrentValue = false,
    Flag = "ClickDetectors",
    Callback = function(val)
        clickEnabled = val
    end,
})

CombatTab:CreateToggle({
    Name = "Delete Fake Objects",
    CurrentValue = false,
    Flag = "DeleteFake",
    Callback = function(val)
        fakeEnabled = val
        fakeTimer = 0
    end,
})

CombatTab:CreateToggle({
    Name = "Delete Wraith Objects",
    CurrentValue = false,
    Flag = "DeleteWraith",
    Callback = function(val)
        wraithEnabled = val
        wraithTimer = 0
    end,
})

CombatTab:CreateToggle({
    Name = "Delete FakeWalls",
    CurrentValue = false,
    Flag = "DeleteFakeWalls",
    Callback = function(val)
        wallsEnabled = val
        wallsTimer = 0
    end,
})

CombatTab:CreateToggle({
    Name = "Delete WeakWalls",
    CurrentValue = false,
    Flag = "DeleteWeakWalls",
    Callback = function(val)
        weakEnabled = val
        weakTimer = 0
    end,
})

-- ============================================================
-- VISION TAB
-- ============================================================

VisionTab:CreateSection("Vision")

VisionTab:CreateToggle({
    Name = "Wraith ESP",
    CurrentValue = false,
    Flag = "WraithESP",
    Callback = function(val)
        espEnabled = val
        if espEnabled then
            startSubLevelWatcher()
            local sub = workspace:FindFirstChild("Sublevel")
            if sub then scanContainer(sub) end
        else
            clearESP()
        end
    end,
})

VisionTab:CreateToggle({
    Name = "Hide Players",
    CurrentValue = false,
    Flag = "HidePlayers",
    Callback = function(val)
        hideEnabled = val
        if hideEnabled then
            for _, p in ipairs(Players:GetPlayers()) do applyHideToPlayer(p) end
            playerAddedConn = Players.PlayerAdded:Connect(applyHideToPlayer)
        else
            if playerAddedConn then playerAddedConn:Disconnect() end
            for _, p in ipairs(Players:GetPlayers()) do restorePlayer(p) end
        end
    end,
})

-- ============================================================
-- UTILITY TAB
-- ============================================================

UtilityTab:CreateSection("Teleport")

UtilityTab:CreateButton({
    Name = "Teleport to Exit",
    Callback = function()
        pcall(function()
            local part = getExitPart()
            if part then
                player.Character.HumanoidRootPart.CFrame = part.CFrame + Vector3.new(0, 5, 0)
            end
        end)
    end,
})

UtilityTab:CreateButton({
    Name = "Teleport to Alt Exit",
    Callback = function()
        pcall(function()
            local part = getAltExitPart()
            if part then
                player.Character.HumanoidRootPart.CFrame = part.CFrame + Vector3.new(0, 5, 0)
            end
        end)
    end,
})

UtilityTab:CreateSection("Automation")

UtilityTab:CreateToggle({
    Name = "Auto Beat Sublevels",
    CurrentValue = false,
    Flag = "AutoBeatSublevels",
    Callback = function(val)
        promptEnabled = val
    end,
})

UtilityTab:CreateToggle({
    Name = "Auto Beat Alt Exits",
    CurrentValue = false,
    Flag = "AutoBeatAltExits",
    Callback = function(val)
        altPromptEnabled = val
    end,
})

-- ============================================================
-- MAIN HEARTBEAT
-- ============================================================

RunService.Heartbeat:Connect(function(dt)
    if clickEnabled then fireClicks() end

    if fakeEnabled then
        fakeTimer += dt
        if fakeTimer >= 2 then fakeTimer = 0 deleteFake() end
    end

    if wraithEnabled then
        wraithTimer += dt
        if wraithTimer >= 2 then wraithTimer = 0 deleteWraith() end
    end

    if wallsEnabled then
        wallsTimer += dt
        if wallsTimer >= 2 then wallsTimer = 0 deleteFakeWalls() end
    end

    if weakEnabled then
        weakTimer += dt
        if weakTimer >= 2 then weakTimer = 0 deleteWeak() end
    end

    if promptEnabled then
        local prompt = getExitPrompt()
        if prompt then
            prompt.MaxActivationDistance = math.huge
            prompt.RequiresLineOfSight = false
            local hrp = player.Character and player.Character:FindFirstChild("HumanoidRootPart")
            if hrp then
                hrp.CFrame = prompt.Parent.CFrame
                fireproximityprompt(prompt)
            end
        end
    end

    if altPromptEnabled then
        local prompt = getAltExitPrompt()
        if prompt then
            prompt.MaxActivationDistance = math.huge
            prompt.RequiresLineOfSight = false
            local hrp = player.Character and player.Character:FindFirstChild("HumanoidRootPart")
            if hrp then
                hrp.CFrame = prompt.Parent.CFrame
                fireproximityprompt(prompt)
            end
        end
    end
end)

workspace.ChildAdded:Connect(function(child)
    if child.Name == "Sublevel" then
        task.defer(function()
            if wraithEnabled then
                for _, obj in ipairs(child:GetDescendants()) do
                    if obj.Name:lower():find("wraith") then pcall(function() obj:Destroy() end) end
                end
            end
            if weakEnabled then
                for _, obj in ipairs(child:GetDescendants()) do
                    if obj.Name:lower():find("weak") then pcall(function() obj:Destroy() end) end
                end
            end
        end)
    end
end)
